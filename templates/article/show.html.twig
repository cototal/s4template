{% extends 'base.html.twig' %}

{% block title %}{{ article.title }}{% endblock %}

{% block body %}
    <div class="row mt-4">
        <div class="col-md-4 mb-4">
            <h1>{{ article.title }}</h1>
        </div>
        <p>{{ article.content }}</p>
    </div>
{% endblock %}
